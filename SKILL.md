---
name: de-ai-flavor
description: "中文写作去 AI 味检测工具（通用版）。当用户说「去AI味」「去 AI 味」「AI味太重」「帮我检查AI味」「这段像AI写的」等触发。扫描文本 → 输出逐条改写建议 → 用户决定采纳，绝不直接改原文。"
---

# de-ai-flavor 去 AI 味工具（通用版）

## 定位

通用中文「去 AI 味」检测工具：**扫描文本 → 输出逐条改写建议 → 用户决定采纳。** 面向报告、邮件、说明文等日常文档。

- 绝不直接改原文，只给建议
- 不含任何个人风格指纹，可安全公开
- 中文场景优先

## 快速使用

```bash
bash run.sh scan 文章.md
cat 文章.md | bash run.sh scan
```

## 扫描结果解读

```
[de-ai-flavor] 扫描完成
═══ 硬性违规 (block) ═══   ← 绝对禁用
第12行 | 本质上 | → 说到底 / 其实
═══ 建议 (warn) ═══        ← 风格建议
第30行 | 首先 | → 直接说第一件事
```

## 红线

- 绝不直接改原文，只输出建议
- 反幻觉：建议只给真实可行的改写方向，改不出就留白
- 中文优先，标点用中文全角
- 不调外部模型（现阶段）

## ⚠️ 语料分层：命中 ≠ 真 AI 味（2026-08-14 实测）

本工具规则为**报告/邮件/日常文档**设计。对**创作型内容**（文学非虚构、科普、人物特稿、分析文章）会**过度反应**：

- 「本质上」「这意味着」「闭环」在科普/叙事语境里往往是**精确表达**，不是 AI 味（实测：The-Human-Remainder 45 处命中、business-model-forensics 7 处命中，绝大多数是合理用法）
- 扫描**规范文档/批阅指南/禁用词清单**会**自指误报**（清单里的词被当成违规）
- 自动生成内容（日报/榜单/报告）的 AI 味根因在**生成 prompt**，改成品是治标

**动手前必须逐句甄别**，用三问法：①删掉信息有损吗（无损=填充=AI味）②总结升华还是精确表述（精确=保留）③换口语更短吗（更短=AI味）。

完整观察与处理优先级：`docs/corpus-reactions-v1.md`（语料类型 × 反应 × 策略表）。
